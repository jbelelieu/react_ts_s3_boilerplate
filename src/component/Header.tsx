import * as React from "react";
import logo from './../imgs/logo.png';

function Header() {
    return (
        <header>
            <img src={logo} alt="Logo" />
        </header>
    );
}

export default Header;